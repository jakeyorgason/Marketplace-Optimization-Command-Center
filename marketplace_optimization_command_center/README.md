# Marketplace Optimization Command Center

Clean rebuild: one package folder, no duplicate root modules/pages.

Run locally:

```bash
cd marketplace_optimization_command_center
pip install -r requirements.txt
streamlit run app.py
```

Recommended Streamlit Cloud main file:

```text
marketplace_optimization_command_center/app.py
```

Important: delete any old top-level `modules/`, top-level `pages/`, or duplicate app files outside this folder before deploying.
